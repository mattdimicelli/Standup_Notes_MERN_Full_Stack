module.exports = function(router) {  
    // "router" is the express router from index.js of the api folder
    // GET: list of standup meeting notes
    router.get('/standup', function(req, res) {
        
    })
}